// const gridItems = document.querySelectorAll('.project-item');

// gridItems.forEach((item) => {
//   item.addEventListener('mousemove', (event) => {
//     const width = item.offsetWidth;
//     const height = item.offsetHeight;
//     const mouseX = event.pageX - item.offsetLeft - width / 2;
//     const mouseY = event.pageY - item.offsetTop - height / 2;
//     const depth = 25;
    
//     item.style.transform = `translateX(${-mouseX / depth}px) translateY(${-mouseY / depth}px)`;
//   });
  
//   item.addEventListener('mouseleave', () => {
//     item.style.transform = 'translate(0, 0)';
//     item.style.transition = 'ease-in 0.2s'
//   });
// });
